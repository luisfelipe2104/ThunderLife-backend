// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Commands
// npm init -y
// yarn add prisma typescript ts-node @types/node --save-dev
// npx prisma init
// npx prisma migrate dev --name youtube_channels_table

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model User {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  name String 
  email String @unique
  password String
}

model Habit {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  user_id String
  habitName String
  habitDescription String?
  habitGoal String
}

model PositiveStreak {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  habit_id String
  date String
  comment String?
}

model NegativeStreak {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  habit_id String
  date String
  comment String?
}

model PartialStreak {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  habit_id String
  date String
  comment String?
}